# Fixture gate config — ground truth for the "N release-commit-only checks" claim.
# Exactly two rows below are release-commit ONLY (beta, gamma); alpha is two-phase and
# delta is pre-push, so neither counts. Blank lines and comments must be skipped.

alpha  | checks/alpha.sh  | work-commit,release-commit | block |
beta   | checks/beta.sh   | release-commit             | block |
gamma  | checks/gamma.sh  | release-commit             | warn  |
delta  | checks/delta.sh  | pre-push                   | block | npm test
